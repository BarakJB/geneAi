version: 1
applications:
  - appRoot: /
    frontend:
      phases:
        preBuild:
          commands:
            - cd client
            - npm ci --no-audit --no-fund
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: client/dist
        files:
          - '**/*'
      cache:
        paths:
          - client/node_modules/**/*

